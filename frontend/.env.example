# Frontend env (baked in at `npm run build`)
# Leave unset for local dev — Vite proxies /ml-api → localhost:8000

# Production (Render Static Site):
# VITE_ML_API_URL=https://YOUR-ML-SERVICE.onrender.com/api/v1/ml

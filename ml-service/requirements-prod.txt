# Production / Render Web Service — runtime only (no Jupyter, Prophet, lint tools)
# Build: pip install -r requirements-prod.txt
# Start:  uvicorn src.main:app --host 0.0.0.0 --port $PORT

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# ML & Data Science (runtime inference)
# Pin versions close to training environment so .pkl models load on Render
scikit-learn>=1.5.0,<1.7.0
pandas>=2.2.0,<3
numpy>=1.26.0,<2.3
joblib>=1.3.2

# Database
psycopg[binary]>=3.1.18
sqlalchemy>=2.0.25

# Optional cache
redis>=5.0.1

# Utilities
pydantic>=2.5.0
pydantic-settings>=2.1.0
twilio>=8.10.0
